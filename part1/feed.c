#include <stdio.h>
#define COLUMNS 2

int main() {
	int n;
	scanf("%d", &n);
	int numbers[n][COLUMN];
	for (int i = 0; i < n; i++) {
		for (int j = 0; j < COLUMN; j++) {
			scanf("%d", &numbers[i][j]);
		}
	}

	
}
